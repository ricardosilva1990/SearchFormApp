@testable import SearchFormApp

struct StationViewModelMocks {
    static let porto = StationViewModel(stationModel: StationModelMocks.porto)
    static let barcelona = StationViewModel(stationModel: StationModelMocks.barcelona)
    static let madrid = StationViewModel(stationModel: StationModelMocks.madrid)
}
